# AgentQ — 프로젝트 AI 작업 규칙

> 이 파일은 `docs/_agents-template.md`(범용 템플릿)를 이 프로젝트에 맞게 채운 것이다.
> 아직 정해지지 않은 항목은 `[결정 필요]`로 표시했다. **추측으로 채우지 않는다.**

## 1. 문서 적용 우선순위

1. 이 `AGENTS.md`
2. `docs/ai-development-handoff-guidelines.md`
3. 기능별 기획 문서 — **아직 없음(§3 참조)**
4. 디자인 또는 화면 명세 — **아직 없음**
5. API 계약과 데이터 명세 — `docs/API-PROPOSAL.md` (프론트 제안, 백엔드 확정 대기)
6. 테스트 및 배포 문서 — `docs/` (작성 중)

문서가 충돌하면 더 구체적이고 현재 작업에 가까운 문서를 우선한다. 판단이 어렵거나
결과가 크게 달라질 수 있으면 임의로 결정하지 말고 사용자에게 알린다.

## 2. 프로젝트 개요

- 프로젝트명: AgentQ
- 제품 목적: 조직 업무에 생성형 AI 에이전트를 붙이는 플랫폼. 발주처(도메인)를
  갈아끼울 수 있는 구조가 핵심이다.
- 주요 사용자: 실무 담당자(사용자 포털) / 플랫폼 운영자(관리자)
- 현재 개발 단계: **P0 진행 중** — 사용자 포털 잔여 11종 중 9종 완료.
  백엔드 미연결. 데이터는 전부 `src/fixtures/`이며 `shared/api/`가 교체 지점이다.
- 지원 환경: 웹(데스크톱 우선, 최소 모바일 375px 지원)
- 기준 언어와 지역: 한국어, Asia/Seoul

### 이 저장소의 위치

이전 구현체(목업 데모)가 별도로 존재한다.

| | 위치 | 상태 |
|---|---|---|
| 이전 데모 | `C:\한국부동산원\genos-app` → https://hyegeungim3-git.github.io/qagent-platform/ | **유지·수정 금지 대상 아님. 그대로 살려 둔다** |
| 현재 저장소 | `C:\AgentQ-claude` | 실서비스 전환 준비용 신규 구축 |

이전 데모는 백엔드 없이 전부 mock으로 동작한다. 이 저장소의 목적은 **실제 API를
붙일 수 있는 형태로 다시 세우는 것**이다. 데모의 화면·콘텐츠는 자산으로 가져오되,
데이터 형태는 그대로 옮기지 않는다(§9 참조).

### 현재 작업 목표

`docs/SCOPE-PLAN.md`의 P0 — 사용자 포털 잔여 11종.
완료: 워크스페이스·공지사항·사용 가이드·대화 영구 저장·알림 센터·업무 브리핑·
라이브 지표·XAI 패널·피드백(9종).
남음: 지도 인텔리전스·환경설정(2종).

## 3. 필수 참고 자료

| 자료 | 상태 |
|---|---|
| 제품 요구사항 | **없음** |
| 디자인 기준 | **없음** — 이전 데모 화면이 사실상 기준 |
| 화면 및 URL 목록 | `docs/_inventory.json` (이전 데모 실측) |
| API 문서 | `docs/API-PROPOSAL.md` — 프론트가 제안. 백엔드 확정 전 |
| 데이터 정책 | **없음** |
| 테스트 기준 | 이 문서 §14 |
| 배포 문서 | `[결정 필요]` |

**참고 자료가 없으면 AI가 내용을 추측해 확정하지 않는다.** 구현 가능한 범위와
결정이 필요한 범위를 구분해 보고한다.

API는 **프론트가 제안하고 백엔드가 확정한다**(D-010). 제안서를 쓰는 것은 허용되지만
**확정된 것처럼 쓰지 않는다.** 인증·권한·테넌시처럼 프론트가 정할 수 없는 것은
비워 두고 질문으로 남긴다(`docs/API-PROPOSAL.md` §3).

## 4. 작업 시작 전 필수 절차

1. 이 문서와 `docs/ai-development-handoff-guidelines.md`를 끝까지 읽는다.
2. 저장소 구조와 기존 구현 패턴을 확인한다.
3. 비슷한 화면·기능·데이터 처리 코드가 있는지 찾는다.
4. `git status`로 현재 변경 사항을 확인하고 사용자 작업을 보존한다.
5. 요구사항과 기존 코드·문서의 충돌 여부를 확인한다.
6. 구현 범위, 검증 방법, 예상 변경 파일을 정리한다.
7. 새로운 기술 결정이나 권한이 필요한 작업은 구현 전에 알린다.

## 5. 프로젝트 구조

```text
AgentQ-claude/
├─ src/
│  ├─ app/        앱 초기화, 라우팅, 전역 오류 처리
│  ├─ pages/      URL로 접근하는 화면
│  ├─ widgets/    여러 기능을 조합한 큰 화면 영역
│  ├─ features/   사용자 행동과 업무 기능
│  ├─ entities/   핵심 도메인 데이터와 타입
│  ├─ shared/     여러 도메인에서 재사용하는 기반 코드
│  ├─ fixtures/   예시 데이터 (실제 데이터 연결부와 분리)
│  └─ test/       테스트 설정
├─ e2e/           Playwright E2E
├─ scripts/       검증·조사 스크립트
└─ docs/          기획·결정·인수인계 문서
```

### 영역별 책임

| 영역 | 책임 | 참조 가능 범위 |
|---|---|---|
| `app` | 초기화·라우팅·전역 오류 | 모든 하위 영역 |
| `pages` | 라우트 해석·화면 조합 | widgets, features, entities, shared |
| `widgets` | 큰 화면 영역 조합 | features, entities, shared |
| `features` | 업무 기능 단위 | entities, shared |
| `entities` | 도메인 데이터·타입 | shared |
| `shared` | 기반 코드(UI·유틸·데이터 접근) | 없음 — 상위를 참조하지 않는다 |
| `fixtures` | 예시 데이터 | entities(타입만) |

**`shared`가 특정 페이지나 업무 도메인을 알면 안 된다.** 경로 별칭(`@shared` 등)을
쓰는 이유가 이것이다 — 상대경로 `../../../`는 의존 방향을 숨긴다.

## 6. 구현 원칙

가이드 §3~4를 따른다. 이 프로젝트에서 특히 중요한 것:

- 계산 가능한 값을 별도 상태로 저장하지 않는다.
- **부모 컴포넌트 안에서 하위 컴포넌트를 정의하지 않는다.** 매 렌더마다 새 타입이
  되어 리마운트되고, 입력창이라면 첫 글자만 입력된다. ESLint
  `react-hooks/static-components`가 잡는다 — 이전 데모에서 실제로 난 사고다.
- 훅을 조건부로 호출하지 않는다(`rules-of-hooks`).
- 렌더 중 `Date.now()` 같은 impure 호출을 하지 않는다(`purity`).

## 7. 기술 및 라이브러리 선택

**신규 프로젝트이므로 React + TypeScript(엄격 모드)를 기본으로 한다.**
`tsconfig.app.json`에 `strict`·`noUncheckedIndexedAccess`·`exactOptionalPropertyTypes`를
켜 두었다. `any`·`@ts-ignore`로 우회하지 않는다.

현재 채택 기술:

| 목적 | 선택 | 비고 |
|---|---|---|
| 빌드 | Vite | |
| UI | React 19 | |
| 언어 | TypeScript strict | |
| 스타일 | Tailwind CSS v4 | |
| 린트 | **ESLint** (+ react-hooks v7) | 스캐폴드 기본 oxlint에서 교체 — 근거는 `eslint.config.js` 주석 |
| 단위·화면 테스트 | Vitest + Testing Library | |
| E2E | Playwright | 데스크톱 1440 · 모바일 두 폭 |

**AI는 승인 없이 새 라이브러리를 설치하지 않는다.** 필요하면 문제·선택지·장단점·
영향 범위를 먼저 제시한다. 확정된 결정은 `docs/DECISIONS.md`에 기록한다.

## 8. 퍼블리싱 단순성 원칙

가이드 §8을 그대로 적용한다. 기획에 없는 단계·탭·모달·URL을 추가하지 않는다.
재사용 가능성이 불확실한 코드를 미리 범용화하지 않는다.

## 9. 상태 및 데이터 관리

### fixture 규칙 (이 프로젝트의 핵심 제약)

이전 데모의 mock은 **UI 표시용 형태**였다. 예:

```js
{ label: '수집 항목', value: '4,380건', sub: '...', tone: 'base' }
```

`tone: 'base'`는 서버가 줄 값이 아니다. 서버는 숫자를 주고 색은 화면이 정한다.
**이 형태를 그대로 옮기면 API를 붙일 때 전부 다시 써야 한다.**

따라서 이 저장소의 fixture는 다음을 지킨다.

- fixture는 **API 응답이 될 수 있는 형태**로 쓴다(원시 값·코드·ID).
- 표시용 변환(단위·색·라벨)은 화면 쪽에서 한다.
- `entities/`에 타입을 정의하고 fixture가 그 타입을 만족하게 한다 —
  스키마 오작성이 컴파일 타임에 잡힌다.
- fixture를 인증·권한의 근거로 쓰지 않는다.
- 개인정보는 명백한 가상 값만 쓴다.

### API 경계

- 페이지에서 API 주소·요청 세부사항을 직접 반복하지 않는다.
- 데이터 접근은 `shared/api/`를 통과한다. 지금은 fixture를 반환하고,
  실제 서버가 정해지면 이 경계 안에서만 교체한다.
- 로딩·빈 응답·오류·재시도 상태를 처리한다.
- 실패를 성공으로 표시하거나 조용히 fixture로 대체하지 않는다.
- **API 제안은 `docs/API-PROPOSAL.md`에만 쓴다.** 화면이 실제로 쓰는 것에서
  끌어내고, 프론트가 정할 수 없는 것(인증·권한·테넌시 등)은 비워 둔다.
- 제안서에 요청·응답 형태를 다시 적지 않는다. `entities/*/model.ts`를 가리킨다 —
  두 곳에 적으면 반드시 갈라진다. `shared/api/contract.test.ts`가 양방향으로 지킨다.

## 10~13. 화면·폼·라우팅·보안

가이드 `docs/ai-development-handoff-guidelines.md` §7·8·11을 따른다.
프로젝트 고유 규칙이 생기면 여기에 추가한다.

## 14. 테스트 및 검증

### 프로젝트 명령

```text
개발 서버:        npm run dev
정적 검사:        npm run lint
타입 검사:        npm run typecheck
단위/화면 테스트:  npm run test
E2E 테스트:       npm run e2e
production build: npm run build
미리보기:         npm run preview
전체 검증:        npm run verify   (lint → typecheck → test → build)
```

**위 명령은 전부 실제로 실행해 통과를 확인했다.** 존재하지 않는 명령을 만들지 않는다.

### ⚠️ 경로 제약 (반드시 지킬 것)

**프로젝트 경로에 한글이 들어가면 안 된다.** 모듈 수가 늘어나면
`vite build`가 `transformed` 직후 네이티브 크래시(`0xC0000409`)로 조용히 죽는다.

실측 근거:

| 경로 | 모듈 | 결과 |
|---|---|---|
| 한글 경로 (최소 프로젝트) | 30 | 통과 |
| 한글 경로 (동일 코드, 무거운 의존성) | 2,357 | **크래시** |
| ASCII 경로 (**같은 코드**) | 2,357 | 통과 |
| 이전 데모 `C:\한국부동산원\genos-app` | 2,437 | **크래시** |

**작을 때는 멀쩡하다가 커지면 죽는다.** 이전 프로젝트가 이 함정을 7주간 못 잡았다.
그래서 이 저장소는 `C:\AgentQ-claude`에 둔다. 폴더를 옮길 때 한글 경로로 가지 말 것.

### 검증 기준

- 변경 범위에 맞는 lint·typecheck를 실행한다.
- 핵심 업무 규칙과 사용자 상호작용을 테스트한다.
- 주요 흐름을 E2E로 확인한다(데스크톱·모바일 두 폭).
- production build를 확인한다.
- **실행하지 못한 검증은 이유와 후속 조치를 보고한다.** 통과했다고 쓰지 않는다.

## 15. 완료 조건

가이드 §14의 체크리스트를 따른다. 확인하지 못한 항목이 있으면
**구현 완료와 검증 완료를 구분해서** 보고한다.

## 16. Git 및 파일 변경 안전 규칙

- 작업 시작·종료 시 변경 파일을 확인한다.
- 관련 없는 사용자 변경을 수정·삭제하지 않는다.
- **커밋·push·배포는 사용자가 요청한 경우에만 수행한다.**
- 빌드 산출물·임시 파일을 저장소에 남기지 않는다.
- 이전 데모 저장소(`qagent-platform`)는 이 작업의 대상이 아니다. 건드리지 않는다.

## 17. 작업 문서 관리

| 문서 | 경로 | 상태 |
|---|---|---|
| AI 개발 가이드 | `docs/ai-development-handoff-guidelines.md` | 있음 |
| 범용 템플릿 원본 | `docs/_agents-template.md` | 있음 |
| 이전 데모 실측 목록 | `docs/_inventory.json` | 있음 |
| 기술 결정 기록 | `docs/DECISIONS.md` | 있음 (D-001~D-010) |
| API 제안서 | `docs/API-PROPOSAL.md` | 있음 — 백엔드 확정 대기 |
| 범위 계획 | `docs/SCOPE-PLAN.md` | 있음 (P0~P4, 85종) |
| 재구축 기록 | `docs/REBUILD-NOTES.md` | 있음 — 이전 데모와 달라진 점·이유. 작업마다 갱신 |
| 현재 작업 목록 | 이 문서 §20 | 별도 파일을 만들지 않았다 — 항목이 적어 나눌 이유가 없다 |

## 18. AI 완료 보고 형식

가이드 §15 형식을 따른다. "완료했습니다"만 남기지 않는다.

## 19. 프로젝트별 추가 규칙

### 금지 사항

- 프로젝트 경로에 한글 사용(§14 참조)
- 부모 컴포넌트 내부에서 하위 컴포넌트 정의
- 확정되지 않은 API를 확정된 것처럼 쓰기 (제안은 `docs/API-PROPOSAL.md`에만)
- fixture를 UI 표시 형태로 작성(§9 참조)
- 이전 데모 저장소 수정

### 업무 규칙 / 디자인 규칙 / 데이터·권한 규칙 / 배포 규칙

`[결정 필요]` — §3의 참고 자료가 확보돼야 작성 가능하다.

## 20. 현재 작업

- **현재 목표**: 사용자 포털 이식 완료(에이전트 13종 + 복합 업무 릴레이 1종).
  다음은 백엔드 연결이다.
- **결정 사항**:
  1. ~~**API 계약 주체**~~ — **정해짐(2026-08-02): 프론트가 제안하고 백엔드가 확정.**
     제안서는 `docs/API-PROPOSAL.md`, 결정 배경은 D-010.
     이제 남은 것은 **백엔드의 확정 회신**이다. 회신이 오면 `shared/api`의
     `TODO(api-미확정)` 22곳을 실제 호출로 바꾼다 — 화면은 손대지 않는다.
     업로드(`uploadDocument`·`uploadDataset`)는 그때까지 실패만 반환한다(D-009).
  2. **발주처 4종 유지 여부** — 업무 데이터가 있는 발주처는 제조(한빛정밀)
     하나뿐이라, 나머지 셋은 `status: 'planned'`로 선택을 막아 두었다.
     4종을 다 살리면 에이전트 13종이 쓰는 fixture가 4배가 된다.
  3. ~~**관리자 화면 범위**~~ — **정해짐(2026-08-02): 잘라내지 않고 전부 만든다.**
     실측 결과 관리자 메뉴는 **74개**(61개가 아니었다). 사용자 포털 잔여 11종까지
     합쳐 85종이며, 순서는 `docs/SCOPE-PLAN.md`에 P0~P4로 정리했다.
     P0(포털 잔여) → P1(관리자 셸+대시보드) → P2(운영·관리) → P3(AI 서비스) → P4(인프라).
     P4를 마지막에 둔 이유는 GPU·노드 수치가 서버 없이는 거짓 계기판이 되기 때문이다.
  4. **디자인 기준** — 원본이 없으면 이전 데모 화면이 기준이다
- **완료된 작업**: 저장소 골격(TS strict · ESLint · Vitest · Playwright · CI 게이트 · Pages 배포),
  한글 경로 함정 실측·회피, 포털·허브, **에이전트 13종 전체 이식**,
  공통 실행 훅(useAgentRun)·화면 골격(AgentShell) 추출, Recharts 지연 로딩,
  파일 업로드 경로(D-009), **복합 업무 오케스트레이션 1종**, **채팅 셸(사이드바·3탭·최근 대화)**,
  이전 데모 대비 축소돼 있던 입력 옵션 6종 복원(OCR·지식 검색·표준 보고서·기준정보·회의록·번역)

- **이전 데모 대비 아직 없는 것**: 사용자 포털 11종(워크스페이스·공지사항·사용 가이드·
  알림 센터·라이브 지표·업무 브리핑·지도·XAI 패널·피드백·환경설정·대화 영구 저장)과
  관리자 74종. **전부 만든다** — 순서는 `docs/SCOPE-PLAN.md`.
  이전에 '동작이 없어 옮기지 않았다'고 한 항목들은 **동작을 만들어서** 넣는다.
  껍데기를 복제하지 않는다는 원칙은 그대로다.
- **제외 범위**: 이전 데모(genos-app / qagent-platform) 수정
